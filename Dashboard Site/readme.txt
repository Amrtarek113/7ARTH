to open React app project
check the dependencies in the package json file and install it using this command


npm install dependencies names

to open the react app to be opened into browser open the terminal in the vs code choose the frontend folder then type this command

cd frontend
npm start

for the backend choose the backend in another terminal bash and write this command to open the backend engine

cd backend
python appx.py